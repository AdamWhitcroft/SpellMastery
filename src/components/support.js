import React from "react";

import styles from "../styles/modules/support.module.scss";

const Support = () => {
  return (
    <div className={styles.support}>
      <p>
        Thanks for reading! SpellMastery is entirely free, does not run ads or
        sponsored content. If you'd like to help support this site, and it's
        author, consider becoming a Patron.
      </p>
    </div>
  );
};

export default Support;
